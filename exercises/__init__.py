"""Hands-on exercises for students, built on the bert_cpu autograd engine.

The engine is the trusted foundation; here you *extend* it. See README.md.

- ``q01`` … ``q04`` are **Jupyter notebooks**: fill in the ``forward`` (or, in
  Exercise 01, the ``_backward``) of each cell and run the grading cell below it.
  Start Jupyter from the project root and open ``exercises/q01_activations.ipynb``.
- ``grading.py`` is the finite-difference harness all four notebooks share.
- ``task_*.py`` are complete, runnable training capstones (not fill-in exercises),
  e.g. ``python -m exercises.task_learn_embedding``.
"""
